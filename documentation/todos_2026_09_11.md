# todos 2026-09-11

## context

just applied the patches apdopting cards for the account page and finalized the view / edit cards.


## additional permission checks
* I'm thinking that we should perform additional permission check on the server side for all actions that modify data.


## Things to make sure were removed from the code base
* there was a check and process to clean up "stale" ids. after the backend has been migrated to the database, make
    sure that this code is removed from the code base. it should not be needed anymore.
* Alliance FK: player.alliance_id still has no DB foreign key. Add it when Alliance migrates, along with Kingdom.
* Orphaned time slots: delete_player() can't cascade to time slots while they live in memory. That resolves when time_slot migrates.
* Search scoping: search.py still shows all players to every role. The requirements say results should be alliance-scoped.

## prior to going live TODOs

* ⚠️⚠️⚠️ as noted in schema.py for the Account class, we need to modify the discord_access_token and discord_refresh_token 
  fields to be encrypted in the database. This will require a migration to update the existing data.


## BUGS
* ✅ when loading time zones in the set up wizard, what ever time zones are in the table are not deleted 1st or 
     checked to see if there are duplicates. should we add a region, location unique constraint?
* ✅ after authorizing with discord, the page displayed to collect the admin account's time zone regions / lozation is 
     not within the wizard page, seems jaring to be out of the wizard, then back into the wizard for step 3.
* ✅ additionally, if you click the "back" button on step 3, it takes you back to step 2 in the wizard page, and not 
     the time zone region / location page. It makes you go through the discord authorization again to get to the time 
     zone region / location page.
* ✅ in the wizard, on the admin account step (#2), you click the "continue with discord" button, it redirects you to 
     the discord OAuth authorize page. this is fine. however, if you click the "cancel" button on that page, it 
     redirects you to the non-wizard account registration page. it should take you back to step 2 of the wizard.
* ✅ somehow I created multiple accounts with the code previous to patch 0003... once patch 0003 is applied see if we
     can recreate.
  * account_name should be unique
  * discord_user_id should be unique
  * should have a check constraint on account_type ???
  * ❌ previously we had an index on lower(account_name)... 
* ⚠️ rate limit does not appear to be working on the players page. when you hit ten, you can sill perform additional refreshes.


## minor todos

* ✅ for the set-up wizard, should it check if there is any data in the time_zone table and if yes, either offer to skip 
  or delete the existing data before loading the time zone csv file?
* ✅ when we refresh the user's discord details when editing the account it closes the edit card. Can we keep the edit 
     card open and require the user to click the save button to save the changes?
* ✅ after adding a new account then navigating to the players page, there is no content displayed. We should display 
     a message of "⚠️No data available..."
* ✅ the edit time slot and add time slot cards have inconsistent methods for setting the end time.


## ✅ merge account & player pages

* the player page displays some account information as the parent card, then also displays the account's players as child cards.
* the account page displays the account information (just a little more of it) and provides the ability to view / edit the account information and the ability to add manual accounts for admins.

I'm thinking that it would make sense to merge the account and player pages into a single players page that enables the user manage both their account and players.


## ✅ view / edit for players

* view all fields for a player in a view card, just like you can view all fields for an account in a view card.
* editable fields:
  * king shot name
  * power
  * town_center_level
  * discord nick name - update with sync button only
  * discord_guild_avatar_url - update with sync button only
* all other fields are read only.


## ✅ view / edit Time Slots

* view all fields for a time_slot in a view card, just like you can view all fields for an account in a view card.
* editable fields:
  * local_start
  * local_end
  * time_slot_type
  * needs_ review
* all other fields are read only.

## roles

* the super admin role is assigned at the account level
* other admin roles are assigned at the player level


### todo

* ✅ add permission admin screen for power admins (and super admins) to manage the roles assigned to players. note: power 
  admins should not be able to manage the roles assigned to themselves.


### usage

when a user has some kind of admin role assigned to one or more of the players and/or has the super admin role assigned 
to their account, then they should be able to be able to select which player admin role the wish to use

for example if a user is a super admin, and also is an admin for aliance 1 & 2 and a power admin for aliance 3, then 
they should be able to select from the following:

* user - act as a normal user with no admin privileges, can modify their own account and players for all aliances they belong to
* player xxx \[aliance 1]: admin - act as an admin for the aliance 1
* player yyy \[aliance 2]: admin - act as an admin for the aliance 2
* player zzz \[aliance 3]: power admin - act as a power admin for the aliance 3
* super admin - act as a super admin for all aliances

the content and functionality of the pages should change based on the selected role.


## mobile app

we must ensure the the application uses Responsive Web Design (RWD) to ensure that the application is usable on mobile
devices. The app should gracefully adapt its layout, visuals, and behavior to accomodate mobile screens, touch 
interactions, and varying network conditions. This includes:

* flexible grid layouts that adjust to different screen sizes
* touch-friendly UI elements and gestures
* optimized images and media for mobile performance
* responsive navigation patterns
* consideration for varying network speeds and offline capabilities

### priority areas

* nav bar - currently takes up multiple lines on mobile, we should make it a single line with a hamburger menu for the other items.
* filter displays - also take up multiple lines on mobile.

